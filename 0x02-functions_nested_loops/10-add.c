#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * add - checks if char is lowercase
 * @a: The char to be checked
 * @b: The char to be checked
 * Return: 1 if c is lowercase, 0 otherwise
 */
int add(int a, int b)
{
	return (a + b);
}
