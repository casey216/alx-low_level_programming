#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * _abs - checks if char is an alphabet
 * @n: The number to be made absolute
 * Return: absolute value of n
 */
int _abs(int n)
{
	return (abs(n));
}
